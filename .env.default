# This variable is optional, the config.default.yaml would always be the expected config file.
# It's just an example of how to override the default configuration.
CONFIGURATION_FILE_PATH=config.default.yaml

APP_VERSION=1.0.0 # should be set by the image tag during the build process
